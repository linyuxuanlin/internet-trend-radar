{
  "name": "trend-radar-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create trend-radar",
    "db:migrate:local": "wrangler d1 migrations apply trend-radar --local",
    "db:migrate:remote": "wrangler d1 migrations apply trend-radar --remote",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
